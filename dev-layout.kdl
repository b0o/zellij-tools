layout {
    default_tab_template {
        children
        pane borderless=true size=1 {
            plugin location="zjstatus"
        }
    }
    // Scratchpads are opened manually; see README's Local Dev Example.
    tab name="dev" focus=true
    tab name="review"
}
